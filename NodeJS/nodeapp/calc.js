//this is a custom module

// Use export for multiple named exports.
// Use export default for a single main export.

function add(a, b) {
    return a + b;
}
// function subtract(a, b) {
//     return a - b;
// }
export default add; 
